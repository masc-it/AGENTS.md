# Work plans

- Split work into clear phases. Each phase must end with concrete QA checks.
- Give every task a `todo` or `done` status.
- Each group of tasks must produce working vertical behavior at every checkpoint.
