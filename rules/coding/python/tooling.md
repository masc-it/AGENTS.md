# Python tooling

- Use `uv run ...` when running Python scripts, modules, or inline code.
- For temporary or probe scripts, when no `.venv` exists, add PEP 723 frontmatter with the required dependencies and run the script with `uv`.
