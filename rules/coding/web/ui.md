# Web UI

- With Tailwind and HTMX, define reusable style patterns and components instead of styling elements independently.
- Common elements such as buttons, tables, and form controls must share reusable definitions so the interface remains consistent and styles are not duplicated.
- Prefer minimalism and clear labels. The interface must be intuitive and impose no unnecessary mental overhead.
